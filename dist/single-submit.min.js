"use strict";const s=document.querySelectorAll(".btn-single-submit"),r=document.querySelectorAll(".form-single-submit");document.head.insertAdjacentHTML("beforeend",`<style>
    .spinner-container-single-submit {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .spinner-border-single-submit {
        height: 1rem;
        width: 1rem;
        z-index: 1;
        border-radius: 50%;
        animation: 0.75s linear infinite spinner-border-single-submit;
        border: 0.2rem solid currentColor;
        border-right-color: transparent;
    }
      
    @keyframes spinner-border-single-submit {
        to {
            transform: rotate(360deg);
        }
    }
</style>`);s.forEach(function(e){let n=e.dataset.loadingText??e.innerHTML;e.addEventListener("click",function(){t(e,n)})});r.forEach(function(e){let n=e.querySelector('button[type="submit"]'),i=e.dataset.loadingText??n.innerHTML;n&&e.addEventListener("submit",function(){t(n,i)})});function t(e,n){e.innerHTML=`
            <span class="spinner-container-single-submit">
                <span>${n}</span>
                <span class="spinner-border-single-submit" role="status"></span>
            </span>
        `,e.disabled=!0}
